@model IEnumerable<proyectoscrum.Models.Materia>

@{
    ViewData["Title"] = "Index";
}

<h1>Materias</h1>

<p>
    <a asp-action="Create">Crear nueva materia</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreMateria)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreMateria)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdMateria">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdMateria">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.IdMateria">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
